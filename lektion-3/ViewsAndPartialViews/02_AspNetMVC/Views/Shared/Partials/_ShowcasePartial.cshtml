@model ShowcaseModel

@if (Model != null)
{
    <section class="showcase">
        <div class="container">
            <div class="content">
                <div class="ingress">@Model.Ingress</div>                
                <div class="title">@Model.Title</div>
                <button class="btn-theme">@Model.ButtonText</button>
            </div>
            <div class="content-image">
                <img src="@Model.ImageUrl" alt="@Model.Title">
            </div>
        </div>
    </section>
}

